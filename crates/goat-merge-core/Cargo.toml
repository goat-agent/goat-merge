[package]
name = "goat-merge-core"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
repository.workspace = true

# This crate is the queue's reasoning and nothing else. It takes a snapshot of what GitHub
# says and returns what should happen next. Adding an I/O, async, or clock dependency here
# moves a decision out of reach of the tests that prove it safe. Don't.
[dependencies]
serde = { workspace = true }
serde_yaml = { workspace = true }
thiserror = { workspace = true }
time = { workspace = true }

[lints]
workspace = true
